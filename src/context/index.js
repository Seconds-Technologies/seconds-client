import React, { createContext } from 'react';

export const GeolocationContext = createContext(null);
export const AmplifyContext = React.createContext(null);
export const KanaContext = React.createContext(null);
export const ProductContext = React.createContext(null);
export const TabContext = React.createContext(null);