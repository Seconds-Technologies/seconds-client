{
	"root": "build/",
	"routes": {
		"/**": "index.html"
	},
	"proxies": {
		"/api": {
			"origin": "${REACT_APP_API_HOST}/api"
		},
		"/server": {
			"origin": "${REACT_APP_SERVER_HOST}/server"
		}
	}
}